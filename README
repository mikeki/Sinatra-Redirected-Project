==========================Sinatra Redirecter==========================

A simple Sinatra app that lets you manage the redirects of a domain subfolders to specific urls loading the urls as a iframes, and saving or editing the redirects on a database.
